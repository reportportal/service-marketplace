apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-marketplace.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: service-marketplace
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
